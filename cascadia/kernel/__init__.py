"""
cascadia.kernel — FLINT OS control plane.
Brand name: FLINT
Module path: cascadia.kernel

Contains:
  flint.py   — FLINT supervision core (process lifecycle, health, restart, shutdown)
  watchdog.py — External FLINT liveness monitor (lives outside supervision tree)
"""
