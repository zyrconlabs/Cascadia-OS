# Paths excluded from `git archive` — i.e. from the customer payload staged
# into the installer DMG (cascadia-os.tar.gz). They stay in the repo for
# development; they simply never ship.
#
# tests/ is excluded because:
#   • Disclosure — the suite documents the security model in detail
#     (licensing, tier gating, entitlements, signing, kill switch, vault
#     encryption, API-key auth, Stripe HMAC), including tampered_manifest /
#     tampered_file fixtures that spell out what the verifier catches.
#   • Dead weight — no test runner ships in the bundled runtime (no pytest in
#     the 49-wheel wheelhouse), so the files are unrunnable on a customer box.
#   • 135 of 673 archive entries (~20% of files, 0.9 MB of 16.1 MB).
#   • It also removes the last hardcoded owner chat id, in
#     tests/test_chief_messaging.py (the runtime sites were fixed in 3ed8b27).
#
# Verified safe: nothing under cascadia/ imports tests/, no runtime code reads
# tests/fixtures or conftest, and no installer script references tests/.
#
# PATTERN NOTE: the bare name is deliberate. `tests/ export-ignore` (trailing
# slash) matches NOTHING in .gitattributes — verified with git check-attr — and
# `tests/**` leaves an empty tests/ entry behind. The bare `tests` marks the
# directory itself, so git archive skips the whole subtree, at any depth.
tests export-ignore

# Vendor-side licensing tooling — excluded from the customer payload.
# setup-stripe.sh MINTS a license_secret into config.json. Because license keys
# are symmetric HMAC (the verify secret IS the signing secret), shipping this
# alongside a key generator gave a customer a one-command path to issue
# themselves any tier. It stays in the repo for vendor use; it just never ships.
scripts/setup-stripe.sh export-ignore

# ── docs/ split ───────────────────────────────────────────────────────────────
# The 26 customer-facing docs still ship (technical guides + the finalised legal
# .md set); README links six of them, so docs/ must never be excluded wholesale.
# Everything below is commercial, internal, or not fit to publish.

# Vendor/business docs — commercial + internal process, not product docs.
docs/business export-ignore
docs/templates export-ignore
docs/woocommerce_setup.md export-ignore
docs/woocommerce_email_template.md export-ignore
docs/apple_pay_setup.md export-ignore
docs/production_checklist.md export-ignore
docs/operator_store_page.md export-ignore
docs/compliance_marketing.md export-ignore
docs/sprint-2a-rfc-audit-2026-05-12.md export-ignore
docs/sprint-2b-implementation-plan.md export-ignore

# Unreviewed legal DRAFTS — every one opens "DRAFT — PENDING LEGAL REVIEW —
# Do not publish without attorney review". Shipping them risks a customer
# treating a draft EULA/ToS as binding. The finalised docs/legal/*.md still ship.
docs/legal/acceptable-use.html export-ignore
docs/legal/depot-developer-agreement.html export-ignore
docs/legal/eula.html export-ignore
docs/legal/privacy-policy.html export-ignore
docs/legal/refund-policy.html export-ignore
docs/legal/terms-of-service.html export-ignore

# RFC specs still in flux — not published to third-party developers yet.
docs/mission-package-spec.md export-ignore
docs/package-canonicalization.md export-ignore

# Vendor-only license SIGNING. Holds the path to the private Ed25519 key and is
# the only code that can mint a licence. A customer node needs the verifier
# (tier_validator.py) and the public bundle, never this.
cascadia/licensing/license_signer.py export-ignore
